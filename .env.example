# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Drizzle — provided by the Vercel-managed Neon integration (vercel env pull)
DATABASE_URL="postgresql://postgres:password@localhost:5432/Nexus"

# Vercel AI Gateway — one key for embeddings + generation.
# On Vercel this can authenticate via the OIDC token; set it locally to run
# ingestion (embeddings) and generation.
AI_GATEWAY_API_KEY=""

# Model routing (provider/model). Defaults live in src/env.js.
GEN_MODEL="zai/glm-5.3-flash"
EMBED_MODEL="openai/text-embedding-3-small"
CONTEXT_MODEL="zai/glm-5.3-flash"

# Reranking (Slice 2) — cross-encoder between RRF fusion and neighbor expansion.
# Off by default; it's the measured add-on. Flip RERANK_ENABLED=true to turn it on globally
# (the eval harness toggles it per-run to measure the lift).
RERANK_MODEL="cohere/rerank-v3.5"
RERANK_ENABLED="false"

# Evals (Slice 2) — LLM judge for the golden-set scorers.
EVAL_MODEL="zai/glm-5.3-flash"

# Whisper fallback (Slice 3) — transcribe videos that have no caption track.
# Off by default; downloads audio at ingest time. Flip WHISPER_FALLBACK=true to enable.
WHISPER_MODEL="openai/whisper-1"
WHISPER_FALLBACK="false"

# Optional: full-catalog channel backfill via the YouTube Data API. Unset → channel discovery
# uses the public uploads RSS feed (recent uploads, no key required).
YOUTUBE_API_KEY=""
